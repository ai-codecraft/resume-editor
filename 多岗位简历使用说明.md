# 多岗位简历使用说明

更新时间：2026-08-07

## 1. 打开编辑器

直接打开 `index.html`，或在当前目录启动静态服务器：

```powershell
python -m http.server 4173
```

然后访问 `http://localhost:4173/`。

## 2. 岗位版本

页面顶部“岗位版本”下拉框提供：

- Java 后端
- Web 前端
- 软件测试 / 测试开发
- AI 应用 / Agent 方向
- Java 全栈
- 软件实施 / 技术支持
- 原始默认简历（备份）

每个岗位版本都以默认个人资料为基础，并独立保存到浏览器 `localStorage`。**每份简历只保留一个旗舰项目**，不再为了增加数量而堆叠本地项目。

项目来源可以是公开开源项目的二次开发方案，但投递前必须确认项目能够运行、简历中的功能能够讲清楚，并遵守原项目许可证。

## 3. 当前旗舰项目方向

- **Java 后端**：影院票务与选座系统；突出并发锁座、事务、Redis、RabbitMQ 延迟消息、状态机和幂等。
- **Web 前端**：AI 数据分析运营后台；突出 Vue 3、TypeScript、Element Plus、Pinia、接口联调和可视化。
- **软件测试**：AI 数据分析平台接口与端到端自动化测试；突出 Rest Assured、Playwright Java、Testcontainers、JUnit 和数据库校验。
- **AI Agent**：企业研究与知识检索 Agent；突出 Python、FastAPI、LangGraph、Tool Calling、RAG、SSE 和降级。
- **Java 全栈**：企业工单与服务运营平台；突出 Java + Vue 的页面、接口、数据库和部署闭环。
- **软件实施 / 技术支持**：多租户业务管理平台部署实施；突出 Linux、Docker、MySQL、Redis、Nginx、日志和交付文档。

当前内置版本以编辑器实际加载的数据文件为准，不再保留历史候选方案文档。

## 4. 导出

- **导出**：导出当前版本 JSON，文件名包含姓名和求职目标。
- **导出 PDF**：先检查右侧预览是否为一页 A4；如有溢出，再点击“适应整页”。
- **resumes/**：保存 6 个预生成 JSON 和对应 Markdown 版本。

## 5. 投递前检查

1. 选择与目标 BOSS JD 最接近的版本。
2. 删除 JD 没有要求、自己不能解释的关键词。
3. 确认项目时间、技术栈和个人贡献与实际准备情况一致。
4. 个人优势部分是“自我评价”，不重复罗列技术栈。
5. AI Agent 项目需准备最小 Demo、架构图和失败降级案例；Java 票务项目需准备锁座、超时释放和幂等处理的讲解。
6. 公开项目二次开发要保留出处，不写原作者的商业成果和指标。
7. 导出 PDF 后检查姓名、联系方式、求职目标、项目日期和页数。

## 6. 数据位置

- 浏览器草稿：`localStorage`
- 内置版本唯一数据源：`D:/项目/resume-editor/js/resume-versions-target.js`
- JSON 备份：`D:/项目/resume-editor/resumes\*.json`
